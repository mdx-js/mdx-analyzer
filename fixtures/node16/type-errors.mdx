{/**
  * This tests diagnostics in an ESM section
  *
  * @param {Props} props
  */}
export function getCount(props) {
  return props.counts;
}

{/**
  * @typedef Props
  * @property {number} count
  */}

This tests diagnostics in a content section {props.counter}
