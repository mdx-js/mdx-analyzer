{
  "properties": {
    "mdx": {
      "title": "MDX language server options",
      "properties": {
        "checkMdx": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Enable error reporting in type-checked MDX files."
        },
        "plugins": {
          "$ref": "https://json.schemastore.org/remarkrc.json#/properties/plugins"
        },
        "languagePlugins": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "markdownDescription": "Volar language plugins to load. Each entry is an import specifier that exports a getLanguagePlugin() function."
        }
      }
    }
  }
}
